<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
    {% for house in house_hot_list %}
<div class="col-lg-3">
     <div class="course">
          <div><a href="/house/{{ house.id }}"><img class='img-fluid img-box' src="/static/img/house-bg1.jpg" alt=""></a></div>
      <div class="course-info">
           <span>{{ house.address }}</span>
      </div>
      <div class="course-info1">
           <span>{{ house.rooms }} - {{ house.area }}平方米</span>
           <span style="color: #3498db">&nbsp&nbsp<i class="fa fa-heart"  aria-hidden="true">&nbsp热度{{ house.page_views }}</i></span>
           <span class="price float-right">￥{{ house.price }}</span>
       </div>
   </div>
</div>
{% endfor %}
</body>
</html>